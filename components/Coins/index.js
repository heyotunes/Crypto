import styles from './Coins.module.css';
const Coins = ({...rest}) => {
    return (
        <div className={styles.coin_search}>
          <h1>Coins</h1>
        </div>

    );
};

export default Coins;